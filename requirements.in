# Core dependencies for Spectra MVP
# This file contains minimal dependencies for basic voice control functionality
# Use: pip-compile requirements.in to generate requirements.txt lockfile

# Phase 0 - Core Voice Recognition
openai-whisper==20231117  # Whisper-based STT (fallback)
faster-whisper>=1.0.3  # Optimized Whisper with CTranslate2 (4x faster)
pyaudio==0.2.14  # Microphone input
numpy==1.26.3  # Array processing (required by whisper)
webrtcvad==2.0.10  # Voice Activity Detection

# TICKET-P3-02: Wake Word Detection for Hands-Free Mode
openwakeword>=0.5.1  # Lightweight wake word detection with custom model support
onnxruntime>=1.16.0  # Required by openwakeword for efficient inference

# TICKET-STT-002: Voice Fingerprinting (Speaker Verification)
resemblyzer==0.1.4  # Lightweight speaker verification for voice filtering

# TICKET-P2-01: MLX Whisper for Apple Silicon (macOS ARM64 only)
# Install manually on Apple Silicon: pip install lightning-whisper-mlx
# Provides instant STT with Neural Engine acceleration (< 500ms for 5s audio)
lightning-whisper-mlx>=0.0.9; sys_platform == "darwin" and platform_machine == "arm64"

# Phase 0 - Text-to-Speech (Neural, High-Quality, Cross-Platform)
piper-tts>=1.2.0  # Piper TTS for high-quality neural voices (offline, fast, cross-platform)
soundfile>=0.12.0  # Audio file I/O (required by piper-tts for playback)

# Phase 0 - Mac Automation
pyautogui==0.9.54  # GUI automation
pyobjc-framework-Cocoa==10.1  # AppleScript on Mac
pyobjc-framework-ApplicationServices==10.1  # Mac automation APIs
pynput==1.7.6  # Alternative input control

# Phase 0 - Core Utilities
pillow==10.2.0  # Screenshot/image handling
pyperclip==1.8.2  # Clipboard operations
python-dotenv>=1.0.0  # Environment configuration
platformdirs>=3.0.0  # Cross-platform directory paths
cachetools>=5.3.0  # TTL cache for memory leak fix (TICKET B1)

# Security - Encryption for sensitive data
cryptography>=41.0.0  # Encryption/decryption for database secrets

# Phase 11 - Monitoring
psutil>=5.9.0  # System and process monitoring

# Phase 12 - Extensibility
watchdog>=3.0.0  # File system monitoring for hot-reload

# UI - Cross-platform overlay
PySide6>=6.6.0  # Qt6 for cross-platform UI support

# TICKET-MEM-001: Semantic Memory (RAG Local)
chromadb>=0.4.0  # Vector database for semantic search
sentence-transformers>=3.0.0  # Embedding models for semantic search (includes all-MiniLM-L6-v2)

# TICKET-APP-002: Reader Mode - Web Content Extraction
trafilatura>=1.6.0,<2.0.0  # Clean HTML extraction and conversion to Markdown

# TICKET-FEAT-002: Task Scheduling & Delayed Actions
schedule==1.2.0  # Lightweight task scheduler for cron-like functionality
